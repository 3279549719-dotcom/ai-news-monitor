# 架构借鉴 — 抓取通道数据化 · 增量幂等 · 通知分发 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 DECISION 采纳范围 A1（抓取通道数据化）+ A2（增量幂等闸）+ A3（通知分发器）+ B1（日志收尾 flush）改造管线，直击「score-0 永久固化、单通道依赖、通知硬编码、日志误判」四个已验证痛点。

**Architecture:** 每个措施独立成模块、独立 commit、可回滚。A1 新增 `src/fetch-chain.js` 通道注册表（`backends` 配置列驱动降级链）；A2 新增 `src/seen.js` 每源环形缓冲（`logs/.seen-ids.json` 持久化）+ 修改 `get_new_urls` RPC 放行 score-0 行；A3 新增 `src/notify.js` 多通道分发器（邮件通道复用 `email.js`）；B1 修 `run-pipeline.js` 日志收尾 flush 竞态。不改数据模型主键语义，`backends`/`SEEN_STORE_PATH`/`NOTIFY_CHANNELS` 均 NULL/缺省兼容旧行为。

**Tech Stack:** Node.js CommonJS、node:test、Supabase（service role）、jsonb 列、Windows Task Scheduler（`scripts/run-pipeline.js`）。

## Global Constraints

- 后端一律 CommonJS（`require` / `module.exports`），不引入 ES Module。
- 测试用 `node --test`，单文件跑 `node --test src/<file>.test.js`；全套跑 `npm run check`（= `lint` + `type-check` + `npm test`，lint:backend 只做语法检查）。
- 数据库写操作走 `.env` 的 `SUPABASE_SERVICE_KEY`（`src/db.js` 单例）。迁移用 `mcp__plugin_supabase_supabase__apply_migration`，project id `udjjjkujdzxcxxiqbeyx` 的核对后使用（查询可用 `execute_sql`）。
- 新增/修改文件只暂存本次任务涉及文件，每个 task 末尾一个 commit（conventional commits：`feat(a1)`, `feat(a2)`, `feat(a3)`, `fix(b1)`, `docs(...)`）。
- `.env*`、`logs/`、`node_modules`、本地产物不入库。`logs/.seen-ids.json` 位于 gitignored 的 `logs/`。
- Windows/Git Bash 路径用 `/e/claude/ai-news-monitor/...`；仓库根是 `E:\claude\ai-news-monitor`。
- 日志格式统一：通道名小写（`[crawl4ai]`/`[direct]`/`[twikit]`），替换旧的 `[Crawl4ai]`/`[Direct]` 前缀——无测试依赖日志文本，可自由改。
- 现有信源不预配 `backends` 也必须有兼容默认链（X→`['twikit','crawl4ai']`，其余→`['crawl4ai','direct']`），保证行为不回退。

---

### Task 1: A1 数据模型 — `keyword_sources.backends` 列 + `loadKeywordSources` 选列

**Files:**
- Modify: `src/store.js:67-77`（`loadKeywordSources` 的 select 加 `backends`）
- DB: `public.keyword_sources` 加 `backends jsonb` 列（无迁移文件，直接 apply_migration）
- Test: 无新测试文件（store 无测试基建）——用 SQL 定义核对 + `npm test` 回归 + node 一行验证

**Interfaces:**
- Consumes: `keyword_sources` 表当前列 `id, keyword_id, rss_url, source_name, tier, enabled, created_at, scrape_url, fetch_type`
- Produces: `loadKeywordSources(keywordId)` 返回行额外含 `backends`（jsonb 或 null），供 Task 2 的 `resolveBackends` 读取

- [ ] **Step 1: 应用迁移（apply_migration）**

```sql
ALTER TABLE public.keyword_sources ADD COLUMN IF NOT EXISTS backends jsonb;
```

- [ ] **Step 2: 验证列存在（execute_sql）**

```sql
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name = 'keyword_sources' AND column_name = 'backends';
-- 期望: 1 行, data_type = jsonb
```

- [ ] **Step 3: `src/store.js` 的 `loadKeywordSources` select 加 backends**

在 `src/store.js:71` 的 `.select(...)` 数组里追加 `'backends'`：

```js
      .select('rss_url, scrape_url, source_name, tier, fetch_type, backends')
```

- [ ] **Step 4: 功能验证 loadKeywordSources 返回 backends**

仓库根执行（`require('dotenv').config()` 由 config.js 加载）：

```bash
node -e "require('./src/store').loadKeywordSources('manchester-united').then(r => console.log(JSON.stringify(r.map(s => ({name: s.source_name, backends: s.backends})), null, 2))).catch(e => { console.error(e.message); process.exit(1); })"
```

期望：每行 `backends: null`（旧数据）。若某行已配 backends 则显示数组。

- [ ] **Step 5: 回归全套检查**

```bash
npm run check
```
期望：lint/type-check/test 全绿（现有 88 个用例不回归）。

- [ ] **Step 6: Commit**

```bash
git add src/store.js
git commit -m "feat(a1): keyword_sources 加 backends jsonb 列，loadKeywordSources 返回 backends"
```

---

### Task 2: A1 抓取通道链 — `src/fetch-chain.js` + `src/search.js` 接入 + 关键源 backends 配置

**Files:**
- Create: `src/fetch-chain.js`
- Create: `src/fetch-chain.test.js`
- Modify: `src/search.js:1-7,98-115`（删 `fetchSourceWithFallback`，改用 `fetchSourceWithChain`）

**Interfaces:**
- Consumes: `crawl4ai.fetchSourceArticles(source)`、`scraper-direct.fetchSource(source)`、`x-fetch.fetchXSourceArticles(source)`、`crawl4ai.isXUrl(url)`（Task 1 的 `source.backends`）
- Produces:
  - `resolveBackends(source) => string[]` — 有 `backends` 配置用之；否则 X URL→`['twikit','crawl4ai']`、普通→`['crawl4ai','direct']`
  - `fetchSourceWithChain(source, registry?) => Promise<Array>` — 按链尝试，首个非空返回；全败返回 `[]`
  - `BACKENDS` — 通道注册表（`crawl4ai`/`direct`/`twikit`，每通道「空结果抛错」以便降级）

- [ ] **Step 1: 写失败的测试 `src/fetch-chain.test.js`**

```js
'use strict';

const { test } = require('node:test');
const assert = require('node:assert/strict');
const { resolveBackends, fetchSourceWithChain } = require('./fetch-chain');

test('resolveBackends：有 backends 配置直接返回', () => {
  assert.deepEqual(resolveBackends({ backends: ['crawl4ai'], scrape_url: 'https://claude.com/blog' }), ['crawl4ai']);
});

test('resolveBackends：无配置 + X URL → twikit 主链', () => {
  assert.deepEqual(resolveBackends({ scrape_url: 'https://x.com/someone' }), ['twikit', 'crawl4ai']);
});

test('resolveBackends：无配置 + 普通 URL → crawl4ai→direct', () => {
  assert.deepEqual(resolveBackends({ scrape_url: 'https://techcrunch.com/x' }), ['crawl4ai', 'direct']);
});

test('fetchSourceWithChain：首通道成功即返回，不再调后续通道', async () => {
  const calls = [];
  const registry = {
    a: async () => { calls.push('a'); return [{ url: 'https://x/1' }]; },
    b: async () => { calls.push('b'); return [{ url: 'https://x/2' }]; },
  };
  const items = await fetchSourceWithChain({ backends: ['a', 'b'] }, registry);
  assert.equal(items.length, 1);
  assert.deepEqual(calls, ['a']);
});

test('fetchSourceWithChain：首通道空数组 → 降级下一通道', async () => {
  const registry = {
    a: async () => [],
    b: async () => [{ url: 'https://x/2' }],
  };
  const items = await fetchSourceWithChain({ backends: ['a', 'b'] }, registry);
  assert.equal(items[0].url, 'https://x/2');
});

test('fetchSourceWithChain：首通道抛错 → 降级下一通道', async () => {
  const registry = {
    a: async () => { throw new Error('boom'); },
    b: async () => [{ url: 'https://x/2' }],
  };
  const items = await fetchSourceWithChain({ backends: ['a', 'b'] }, registry);
  assert.equal(items[0].url, 'https://x/2');
});

test('fetchSourceWithChain：全链失败返回空数组', async () => {
  const registry = {
    a: async () => { throw new Error('boom'); },
    b: async () => [],
  };
  const items = await fetchSourceWithChain({ backends: ['a', 'b'] }, registry);
  assert.deepEqual(items, []);
});

test('fetchSourceWithChain：未知通道名跳过继续，不抛错', async () => {
  const registry = { b: async () => [{ url: 'https://x/2' }] };
  const items = await fetchSourceWithChain({ backends: ['zzz', 'b'] }, registry);
  assert.equal(items[0].url, 'https://x/2');
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
node --test src/fetch-chain.test.js
```
期望：FAIL，`Cannot find module './fetch-chain'`。

- [ ] **Step 3: 写最小实现 `src/fetch-chain.js`**

```js
'use strict';

const crawl4ai = require('./crawl4ai-fetch');
const directFetch = require('./scraper-direct');
const xFetch = require('./x-fetch');

// 通道注册表：每通道取到空结果抛错，让链继续降级。
const BACKENDS = {
  crawl4ai: async (source) => {
    const items = await crawl4ai.fetchSourceArticles(source);
    if (!Array.isArray(items) || items.length === 0) throw new Error('crawl4ai 空结果');
    return items;
  },
  direct: async (source) => {
    const items = await directFetch.fetchSource(source);
    if (!Array.isArray(items) || items.length === 0) throw new Error('direct 空结果');
    return items;
  },
  twikit: async (source) => {
    const items = await xFetch.fetchXSourceArticles(source);
    if (!Array.isArray(items) || items.length === 0) throw new Error('twikit 空结果');
    return items;
  },
};

// 信源 → 有序通道链：优先信源声明的 backends；旧数据（无 backends）按 X/普通 分类给兼容默认。
function resolveBackends(source) {
  if (Array.isArray(source.backends) && source.backends.length > 0) {
    return source.backends;
  }
  return crawl4ai.isXUrl(source.scrape_url) ? ['twikit', 'crawl4ai'] : ['crawl4ai', 'direct'];
}

async function fetchSourceWithChain(source, registry = BACKENDS) {
  const chain = resolveBackends(source);
  let lastErr = null;
  for (const name of chain) {
    const fn = registry[name];
    if (typeof fn !== 'function') {
      lastErr = new Error(`未知 backend: ${name}`);
      console.warn(`  [${source.source_name}] 跳过未知通道 ${name}`);
      continue;
    }
    try {
      const items = await fn(source);
      console.log(`  [${name}] ${source.source_name}: ${items.length} 条`);
      return items;
    } catch (err) {
      lastErr = err;
      console.log(`  [${name}] ${source.source_name} → 降级: ${err.message}`);
    }
  }
  console.error(`  ${source.source_name} 全链失败: ${lastErr ? lastErr.message : '无可用通道'}`);
  return [];
}

module.exports = { BACKENDS, resolveBackends, fetchSourceWithChain };
```

- [ ] **Step 4: 跑测试确认通过**

```bash
node --test src/fetch-chain.test.js
```
期望：7 个用例全 PASS。

- [ ] **Step 5: `src/search.js` 接入 `fetchSourceWithChain`**

删掉 `fetchSourceWithFallback`（原 98-115 行），改 import 并替换调用点：

```js
// 顶部：移除不再用的 fetchSource/crawl4ai/xFetch import，新增：
const { fetchSourceWithChain } = require('./fetch-chain');
```

`searchAll` 的循环里（原 137 行）改为：

```js
    for (const source of ordered) {
      let items = await fetchSourceWithChain(source);
      const tier = source.tier ?? Infinity;
      if (tier > 0) items = capSourceItems(items, MAX_PER_SOURCE);
      combined.push(...items);
    }
```

- [ ] **Step 6: 回归 search 既有测试**

```bash
node --test src/search.test.js
```
期望：`deduplicateByUrl`/`sortSourcesByTier`/`capSourceItems` 全 PASS（它们不测抓取函数，不受影响）。

- [ ] **Step 7: 关键源 backends 配置落地（execute_sql，可逆）**

```sql
-- 单通道：Direct 无效/脆弱，只走 crawl4ai
UPDATE keyword_sources SET backends = '["crawl4ai"]'
WHERE scrape_url LIKE '%claude.com/blog%'
   OR scrape_url LIKE '%anthropic.com/news%'
   OR scrape_url LIKE '%anthropic.com/research%';

-- 双通道：Node 直连可达，crawl4ai 挂了还能 Direct 兜底
UPDATE keyword_sources SET backends = '["crawl4ai","direct"]'
WHERE scrape_url LIKE '%techcrunch.com%'
   OR scrape_url LIKE '%nba.com/mavs/news%';

-- X 账号显式声明主链（等价默认，写了更清晰）
UPDATE keyword_sources SET backends = '["twikit","crawl4ai"]'
WHERE scrape_url LIKE '%x.com/%';
```

验证（execute_sql）：

```sql
SELECT source_name, scrape_url, backends FROM keyword_sources WHERE backends IS NOT NULL ORDER BY source_name;
```

> 若某条配置导致真实抓取出问题，回滚 = `UPDATE keyword_sources SET backends = NULL WHERE ...`（NULL 走兼容默认）。

- [ ] **Step 8: Commit**

```bash
git add src/fetch-chain.js src/fetch-chain.test.js src/search.js
git commit -m "feat(a1): 抓取通道数据化——fetch-chain 通道注册表 + backends 配置驱动降级链"
```

---

### Task 3: A2 环形缓冲 — `src/seen.js`（keyForUrl / SeenRing / SeenStore）

**Files:**
- Create: `src/seen.js`
- Create: `src/seen.test.js`

**Interfaces:**
- Consumes: `src/items.js` 的 `normalizeUrlKey(url)`
- Produces:
  - `keyForUrl(url) => string` — URL 归一化键
  - `class SeenRing` — `has(key)` / `add(key)` / `keys()`，固定容量 FIFO 逐出
  - `class SeenStore` — `SeenStore.load({filePath, capacity})` / `has(sourceKey, urlKey)` / `add(sourceKey, urlKey)` / `save({filePath})`；`capacity` 默认 200
- 供 Task 4 的 `applySeenRing` + `processKeyword` 使用

- [ ] **Step 1: 写失败的测试 `src/seen.test.js`**

```js
'use strict';

const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const os = require('os');
const path = require('path');
const { keyForUrl, SeenRing, SeenStore } = require('./seen');

test('keyForUrl：归一化 URL（去协议/www/query/尾斜杠）', () => {
  assert.equal(keyForUrl('https://www.manutd.com/en/news/a?utm=1#top'), 'manutd.com/en/news/a');
});

test('SeenRing：add/has 基本行为', () => {
  const r = new SeenRing(3);
  r.add('a');
  r.add('b');
  assert.equal(r.has('a'), true);
  assert.equal(r.has('c'), false);
});

test('SeenRing：超容量逐出最旧', () => {
  const r = new SeenRing(3);
  r.add('a'); r.add('b'); r.add('c'); r.add('d');
  assert.equal(r.has('a'), false);
  assert.equal(r.has('b'), true);
  assert.equal(r.has('d'), true);
  assert.equal(r.keys().length, 3);
});

test('SeenRing：重复 add 不增容量（移到尾部）', () => {
  const r = new SeenRing(3);
  r.add('a'); r.add('b'); r.add('c'); r.add('a');
  assert.equal(r.keys().length, 3);
  assert.deepEqual(r.keys(), ['b', 'c', 'a']);
});

test('SeenStore：save → load 往返一致', async () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'seen-'));
  const file = path.join(dir, 'seen.json');
  const s = new SeenStore(200);
  s.add('simon-stone-(x)', keyForUrl('https://x.com/sistoney67/status/1'));
  s.add('simon-stone-(x)', keyForUrl('https://x.com/sistoney67/status/2'));
  await s.save({ filePath: file });
  const loaded = await SeenStore.load({ filePath: file, capacity: 200 });
  assert.equal(loaded.has('simon-stone-(x)', keyForUrl('https://x.com/sistoney67/status/1')), true);
  assert.equal(loaded.has('simon-stone-(x)', keyForUrl('https://x.com/sistoney67/status/9')), false);
  fs.rmSync(dir, { recursive: true, force: true });
});

test('SeenStore：文件缺失 → 空 store，不抛错', async () => {
  const loaded = await SeenStore.load({ filePath: path.join(os.tmpdir(), '__missing-seen__.json'), capacity: 200 });
  assert.equal(loaded.has('x', 'y'), false);
});

test('SeenStore：加载超容量 JSON 只留最近 capacity 条', async () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'seen-'));
  const file = path.join(dir, 'seen.json');
  fs.writeFileSync(file, JSON.stringify({ version: 1, sources: { s: ['k1', 'k2', 'k3', 'k4'] } }));
  const loaded = await SeenStore.load({ filePath: file, capacity: 3 });
  assert.equal(loaded.has('s', 'k1'), false);   // 最旧被逐出
  assert.equal(loaded.has('s', 'k2'), true);
  assert.equal(loaded.has('s', 'k3'), true);
  assert.equal(loaded.has('s', 'k4'), true);
  fs.rmSync(dir, { recursive: true, force: true });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
node --test src/seen.test.js
```
期望：FAIL，`Cannot find module './seen'`。

- [ ] **Step 3: 写最小实现 `src/seen.js`**

```js
'use strict';

const fs = require('fs');
const path = require('path');
const { normalizeUrlKey } = require('./items');

// URL → 去重键：与 items.normalizeUrlKey 同一语义（去协议/www/query/尾斜杠）。
function keyForUrl(url) {
  return normalizeUrlKey(url);
}

// 固定容量环形缓冲：Set 判存在 + 数组保序，超容量逐出最旧，O(1) 均摊。
class SeenRing {
  constructor(capacity = 200) {
    this.capacity = capacity;
    this._set = new Set();
    this._order = [];
  }

  has(key) {
    return this._set.has(key);
  }

  add(key) {
    if (this._set.has(key)) {
      this._bump(key);
      return;
    }
    if (this._order.length >= this.capacity) {
      const oldest = this._order.shift();
      this._set.delete(oldest);
    }
    this._set.add(key);
    this._order.push(key);
  }

  _bump(key) {
    const i = this._order.indexOf(key);
    if (i !== -1) {
      this._order.splice(i, 1);
      this._order.push(key);
    }
  }

  keys() {
    return [...this._order];
  }
}

// 每源一个 SeenRing 的持久化门面：load 惰性失败回退空（文件缺失/损坏不阻断管线）。
class SeenStore {
  constructor(capacity = 200) {
    this.capacity = capacity;
    this.rings = new Map(); // sourceKey -> SeenRing
  }

  static async load({ filePath, capacity = 200 } = {}) {
    const store = new SeenStore(capacity);
    try {
      const parsed = JSON.parse(fs.readFileSync(filePath, 'utf8'));
      for (const [sk, keys] of Object.entries(parsed.sources || {})) {
        const ring = new SeenRing(capacity);
        for (const k of keys) ring.add(k); // 超容量在 add 时自动逐出最旧
        store.rings.set(sk, ring);
      }
    } catch {
      // 首次运行/文件损坏：空 store
    }
    return store;
  }

  ringFor(sourceKey) {
    let ring = this.rings.get(sourceKey);
    if (!ring) {
      ring = new SeenRing(this.capacity);
      this.rings.set(sourceKey, ring);
    }
    return ring;
  }

  has(sourceKey, urlKey) {
    return this.ringFor(sourceKey).has(urlKey);
  }

  add(sourceKey, urlKey) {
    this.ringFor(sourceKey).add(urlKey);
  }

  async save({ filePath } = {}) {
    const out = { version: 1, sources: {} };
    for (const [sk, ring] of this.rings) out.sources[sk] = ring.keys();
    fs.mkdirSync(path.dirname(filePath), { recursive: true });
    fs.writeFileSync(filePath, JSON.stringify(out, null, 2), 'utf8');
  }
}

module.exports = { keyForUrl, SeenRing, SeenStore };
```

- [ ] **Step 4: 跑测试确认通过**

```bash
node --test src/seen.test.js
```
期望：7 个用例全 PASS。

- [ ] **Step 5: Commit**

```bash
git add src/seen.js src/seen.test.js
git commit -m "feat(a2): 每源 seen 环形缓冲——keyForUrl/SeenRing/SeenStore 持久化到 logs/.seen-ids.json"
```

---

### Task 4: A2 接入管线 — `src/config.js` 常量 + `src/index.js`（applySeenRing + fetchCandidates/processKeyword/run 带 seen）

**Files:**
- Modify: `src/config.js:12-66`（加 3 个常量）
- Modify: `src/index.js:9-16,141-161,289-308,314-353,373`（import + applySeenRing + 签名带 seen）
- Test: `src/index.test.js`（加 applySeenRing 用例）

**Interfaces:**
- Consumes: Task 3 的 `SeenStore`/`keyForUrl`；`config.SEEN_RING_SIZE`/`SEEN_STORE_PATH`；现有 `fetchCandidates`/`processKeyword`/`run`
- Produces:
  - `applySeenRing(items, seen) => Array` — 纯函数，剔除 seen 内已见 URL（`i.source` 作源键）
  - `fetchCandidates(keyword, seen)` / `processKeyword(keyword, seen)` — 新增第二个参数
  - `run()` — load seen 一次、末位 save 一次

- [ ] **Step 1: `src/config.js` 加常量**

在 `module.exports` 对象内（`JS_WAIT_MS` 附近）加：

```js
  // A2 增量幂等闸：每源最近 N 条已见 URL 环形缓冲（logs/.seen-ids.json，gitignored）。
  SEEN_RING_SIZE: 200,
  SEEN_STORE_PATH: process.env.SEEN_STORE_PATH || require('path').join(__dirname, '../logs/.seen-ids.json'),
```

顶部已 `require('dotenv')`；`require('path')` 放文件顶部一行即可（`const path = require('path');`）。

- [ ] **Step 2: 写失败的测试（`src/index.test.js` 追加）**

```js
const { SeenStore, keyForUrl } = require('./seen');
const { applyTierFloor, T0_FLOOR, preFilter, applySeenRing } = require('./index');
```

```js
test('applySeenRing：剔除 seen ring 内已分析 URL，保留新 URL', () => {
  const seen = new SeenStore(200);
  const items = [
    { source: 'man-utd-official', url: 'https://www.manutd.com/en/news/a' },
    { source: 'man-utd-official', url: 'https://www.manutd.com/en/news/b' },
  ];
  seen.add('man-utd-official', keyForUrl(items[0].url));
  const kept = applySeenRing(items, seen);
  assert.equal(kept.length, 1);
  assert.equal(kept[0].url, 'https://www.manutd.com/en/news/b');
});

test('applySeenRing：空输入返回空数组，不抛错', () => {
  assert.deepEqual(applySeenRing([], new SeenStore(200)), []);
});
```

- [ ] **Step 3: 跑测试确认失败**

```bash
node --test src/index.test.js
```
期望：FAIL，`applySeenRing is not a function`（导出还没加）。

- [ ] **Step 4: `src/index.js` 实现**

顶部 import 追加（改现有 `require('./config')` 解构行）：

```js
const { RESULT_LIMIT, MIN_SCORE, SEEN_RING_SIZE, SEEN_STORE_PATH } = require('./config');
const { keyForUrl, SeenStore } = require('./seen');
```

`fetchCandidates`（原 141-161 行）改签名并在 fetch 后、filterNewItems 前过 seen：

```js
async function fetchCandidates(keyword, seen) {
  const pipeline = PIPELINES[keyword.type];
  if (!pipeline) {
    console.warn(`  [${keyword.name}] 未知类型 "${keyword.type}"，跳过`);
    return null;
  }

  const isBlog = keyword.type === 'blog';
  const label = isBlog ? keyword.url : `"${keyword.query}"`;
  console.log(`\n[${keyword.name}] ${isBlog ? '抓取' : '搜索'} ${label}`);

  const keywordSources = isBlog ? [] : await loadKeywordSources(keyword.id);

  const allItems = await pipeline.fetch(keyword, keywordSources);
  console.log(`  找到 ${allItems.length} 条`);

  const ringFresh = applySeenRing(allItems, seen);
  if (ringFresh.length < allItems.length) {
    console.log(`  [Seen] ${allItems.length - ringFresh.length} 条近期已分析，跳过`);
  }

  const newItems = await filterNewItems(ringFresh, keyword.id);
  console.log(`  未处理: ${newItems.length}`);

  return newItems;
}
```

新增纯函数（放 `preFilter` 之后）：

```js
/**
 * 增量幂等闸第一道：剔除 seen ring 内近期已分析过的 URL（抓取后、DB 过滤前）。
 * @param {Array} items - 本次抓取的全部候选。
 * @param {SeenStore} seen - 每源环形缓冲。
 * @returns {Array} 仅保留 seen 中未出现的条目。
 */
function applySeenRing(items, seen) {
  if (!items || items.length === 0) return items;
  return items.filter(i => !seen.has(i.source, keyForUrl(i.url)));
}
```

`processKeyword`（原 289-308 行）改签名 + 分析完成后登记 seen（只记进入分析预算的 `slice(0, RESULT_LIMIT)`，避免把超预算未评分的 URL 误标）：

```js
async function processKeyword(keyword, seen) {
  // 阶段 1：抓取候选
  const newItems = await fetchCandidates(keyword, seen);
  if (newItems === null) return [];   // 未知类型
  if (newItems.length === 0) return [];

  // 阶段 2：分析 + 交叉验证
  const toSaveRelevant = await analyzeAndCrosscheck(keyword, newItems);

  // 分析完成后登记本轮进入预算的候选到 seen（崩溃中断不误标；相关 URL 由 DB score>0 兜底）
  for (const it of newItems.slice(0, RESULT_LIMIT)) {
    seen.add(it.source, keyForUrl(it.url));
  }

  // 阶段 3：跨运行去重
  const deduped = await dedupeAgainstRecent(toSaveRelevant, keyword.id, 30);

  // 阶段 4：构造记录
  const records = assembleRecords(keyword, deduped, newItems);

  // 阶段 5：持久化
  await persist(records);

  return deduped;
}
```

`run()`（原 314-353 行）在循环外 load 一次、save 一次：

```js
async function run() {
  console.log('=== AI News Monitor ===');
  console.log(`时间: ${new Date().toLocaleString('zh-CN')}`);

  const keywords = await loadKeywords();
  if (keywords.length === 0) {
    console.log('keywords 表中没有启用的关键词，退出。');
    return;
  }
  console.log(`\n监控 ${keywords.length} 个关键词: ${keywords.map(k => k.name).join(', ')}`);

  const seen = await SeenStore.load({ filePath: SEEN_STORE_PATH, capacity: SEEN_RING_SIZE });
  const sections = [];
  for (const kw of keywords) {
    try {
      sections.push({ keyword: kw, results: await processKeyword(kw, seen) });
    } catch (err) {
      console.error(`\n[${kw.name}] 错误: ${err.message}`);
      sections.push({ keyword: kw, results: [] });
    }
  }
  await seen.save({ filePath: SEEN_STORE_PATH });

  const hasResults = sections.some(s => s.results.length > 0);
  // …后续报告/邮件逻辑不变…
```

`module.exports`（原 373 行）加 `applySeenRing`：

```js
module.exports = { run, buildReport, getKeywordRoots, preFilter, processKeyword, toArticleRecord, applyTierFloor, applySeenRing, T0_FLOOR };
```

- [ ] **Step 5: 跑测试确认通过**

```bash
node --test src/index.test.js
```
期望：原有 4 个用例 + 新增 2 个全 PASS。

- [ ] **Step 6: 功能测试 — 全套回归**

```bash
npm run check
```
期望：全绿（fetch-chain/seen 新用例 + index 新用例都在内）。

- [ ] **Step 7: Commit**

```bash
git add src/config.js src/index.js src/index.test.js
git commit -m "feat(a2): 管线接入 seen ring——fetchCandidates 抓取后剔已见，run() 统一 load/save"
```

---

### Task 5: A2 放行 RPC — `get_new_urls` 只把 score>0 视为已见（score-0 行可重评）

**Files:**
- DB: `public.get_new_urls` 函数体 `CREATE OR REPLACE`
- Modify: `src/store.js:27-33`（filterNewItems 的 docstring 注明语义）

**Interfaces:**
- Consumes: `store.filterNewItems` 当前依赖的 RPC（`get_new_urls(p_keyword_id, p_urls)`）
- Produces: RPC 语义改为「score-0 行不算已见」——修复 Marc Stein 式误杀永久固化，配合 Task 4 的 seen ring 防短期重复分析

- [ ] **Step 1: 写失败的「测试」——用 SQL 核对当前行为（execute_sql）**

```sql
-- 任取一个已存在 score=0 的行，确认当前 RPC 把它当「已见」：
SELECT count(*) AS currently_blocked
FROM public.articles
WHERE score = 0 AND url = ANY(
  (SELECT ARRAY(SELECT url FROM public.articles WHERE score = 0 LIMIT 1))::text[]
);
```
期望：≥1（当前行为：score-0 行阻挡）。这不是自动化测试，是改造前的行为快照。

- [ ] **Step 2: 应用迁移（apply_migration）**

```sql
CREATE OR REPLACE FUNCTION public.get_new_urls(p_keyword_id text, p_urls text[])
 RETURNS TABLE(url text)
 LANGUAGE sql
 SET search_path TO ''
AS $function$
  SELECT unnest(p_urls)
  EXCEPT
  SELECT a.url FROM public.articles a
  WHERE a.keyword_id = p_keyword_id
    AND a.url = ANY(p_urls)
    AND a.score > 0;
$function$;
```

- [ ] **Step 3: 验证函数定义（execute_sql）**

```sql
SELECT pg_get_functiondef('public.get_new_urls(text,text[])'::regprocedure);
```
期望：定义含 `AND a.score > 0`。

- [ ] **Step 4: `src/store.js` 更新 filterNewItems 注释**

把 `filterNewItems` 顶部 docstring 改为：

```js
/**
 * Filter out items whose URL already exists for the keyword as a RELEVANT row
 * (score > 0). score=0 rows are treated as "seen marker" but NOT blocking:
 * a mis-scored URL can be re-analyzed on a later run (A2-2). Short-term
 * re-analysis is damped by the seen ring (src/seen.js).
 * Uses the get_new_urls RPC to avoid PostgREST URL length limits on big arrays.
 * @param {Array} items - Candidate items with a `url` field.
 * @param {string} keywordId - Target keyword id.
 * @returns {Promise<Array>} Only items whose URL is new (or only score-0-marked).
 */
```

- [ ] **Step 5: 功能验证 — 改造后行为（execute_sql）**

```sql
-- 取一个 score=0 的 url 与 keyword，直接调新 RPC，应把它当「新」返回：
WITH pick AS (
  SELECT keyword_id, url FROM public.articles WHERE score = 0 LIMIT 1
)
SELECT * FROM public.get_new_urls(
  (SELECT keyword_id FROM pick),
  ARRAY[(SELECT url FROM pick)]
);
```
期望：返回该 url 1 行（score-0 放行）。

> 端到端确认在 Task 8（真实跑一轮后 score-0 行在下轮可被重评）。

- [ ] **Step 6: Commit**

```bash
git add src/store.js
git commit -m "feat(a2): get_new_urls 放行 score-0 行——修复误杀永久固化（配合 seen ring 防短期重评）"
```

---

### Task 6: A3 通知分发器 — `src/notify.js` + `config.NOTIFY_CHANNELS` + `sendDailyDigest` 改造 + 告警收口

**Files:**
- Create: `src/notify.js`
- Create: `src/notify.test.js`
- Modify: `src/config.js`（加 `NOTIFY_CHANNELS`）
- Modify: `src/email.js:1-16,194-205`（`sendDailyDigest` 走 notify；导出加 `buildDigestPayload`）
- Modify: `scripts/run-pipeline.js:47-69`（`sendAlertEmail` 改走 notify；顺带修复旧 sendEmail 签名误用）

**Interfaces:**
- Consumes: `email.sendEmail({subject,text,html}, opts)` 返回 `{sent, subject?/reason?}`；`config.RECEIVER_EMAIL` 等
- Produces:
  - `resolveChannels(opts) => string[]` — 逗号分隔（默认 `config.NOTIFY_CHANNELS`）
  - `notify(payload, opts) => Promise<{sent, subject?, reason?, results?}>` — 逐通道投递，单通道失败不影响其他
  - `CHANNELS` — 注册表（`email` 通道惰性 `require('./email')`，避免循环依赖）
  - `email.sendDailyDigest(sections, opts)` 对外签名不变（`{sent, subject?, reason?}`）
  - `run-pipeline.js` 告警改走 `notify({subject, html, text})`

- [ ] **Step 1: `src/config.js` 加 `NOTIFY_CHANNELS`**

在 `SEEN_STORE_PATH` 附近加：

```js
  // A3 通知通道（逗号分隔；默认 email）。新增通道 = CHANNELS 注册表加一项 + 这里加名。
  NOTIFY_CHANNELS: process.env.NOTIFY_CHANNELS || 'email',
```

- [ ] **Step 2: 写失败的测试 `src/notify.test.js`**

```js
'use strict';

const { test } = require('node:test');
const assert = require('node:assert/strict');
const { resolveChannels, notify } = require('./notify');

test('resolveChannels：逗号分隔解析', () => {
  assert.deepEqual(resolveChannels({ channels: 'email,telegram' }), ['email', 'telegram']);
});

test('resolveChannels：空串/纯空白 → 空数组', () => {
  assert.deepEqual(resolveChannels({ channels: '' }), []);
  assert.deepEqual(resolveChannels({ channels: ' , ' }), []);
});

test('notify：单通道成功 → sent:true 透传 subject', async () => {
  const registry = { email: async () => ({ sent: true }) };
  const r = await notify({ subject: 'S', text: 'T' }, { channels: ['email'], registry });
  assert.equal(r.sent, true);
  assert.equal(r.subject, 'S');
});

test('notify：某通道失败不影响其他通道', async () => {
  const registry = {
    bad: async () => { throw new Error('boom'); },
    ok: async () => ({ sent: true }),
  };
  const r = await notify({ subject: 'S' }, { channels: ['bad', 'ok'], registry });
  assert.equal(r.sent, true);
  assert.equal(r.results.length, 2);
});

test('notify：全通道失败 → sent:false 带原因', async () => {
  const registry = { email: async () => ({ sent: false, reason: 'SMTP 未配置' }) };
  const r = await notify({ subject: 'S' }, { channels: ['email'], registry });
  assert.equal(r.sent, false);
  assert.equal(r.reason, 'SMTP 未配置');
});

test('notify：无通道 → sent:false 不抛错', async () => {
  const r = await notify({ subject: 'S' }, { channels: [] });
  assert.equal(r.sent, false);
  assert.equal(r.reason, 'no channels configured');
});

test('notify：未知通道名跳过，不抛错', async () => {
  const r = await notify({ subject: 'S' }, { channels: ['zzz'], registry: {} });
  assert.equal(r.sent, false);
});
```

- [ ] **Step 3: 跑测试确认失败**

```bash
node --test src/notify.test.js
```
期望：FAIL，`Cannot find module './notify'`。

- [ ] **Step 4: 写最小实现 `src/notify.js`**

```js
'use strict';

const config = require('./config');

// 通道注册表。email 通道惰性 require('./email')：email.js 顶部 require('./notify')，
// 若 notify 顶层也 require email 会成环；函数内 require 拿到的是已完整加载的缓存模块。
const CHANNELS = {
  email: async (payload, opts = {}) => {
    const email = require('./email');
    return email.sendEmail(payload, opts);
  },
};

// 解析启用的通道名（逗号分隔）。opts.channels 覆盖 config.NOTIFY_CHANNELS（测试用）。
function resolveChannels(opts = {}) {
  const raw = (opts.channels != null ? opts.channels : config.NOTIFY_CHANNELS) || '';
  return raw.split(',').map(s => s.trim()).filter(Boolean);
}

/**
 * 统一通知分发：逐通道投递，单个通道失败不影响其他，绝不向上抛错。
 * @param {{subject:string, text?:string, html?:string}} payload - 通知载荷。
 * @param {{channels?:string[], registry?:Object, config?:Object}} opts
 * @returns {Promise<{sent:boolean, subject?:string, reason?:string, results?:Array}>}
 */
async function notify(payload, opts = {}) {
  const channels = resolveChannels(opts);
  if (channels.length === 0) return { sent: false, reason: 'no channels configured' };

  const registry = opts.registry || CHANNELS;
  const results = [];
  let anySent = false;
  let lastReason = '';
  for (const name of channels) {
    const channel = registry[name];
    if (typeof channel !== 'function') {
      lastReason = `unknown channel: ${name}`;
      continue;
    }
    try {
      const r = await channel(payload, opts);
      results.push({ channel: name, ...(r || {}) });
      if (r && r.sent) anySent = true;
      else if (r && r.reason) lastReason = r.reason;
      else lastReason = `${name} 未确认送达`;
    } catch (err) {
      lastReason = err.message;
      results.push({ channel: name, sent: false, reason: err.message });
    }
  }
  return anySent
    ? { sent: true, subject: payload.subject, results }
    : { sent: false, reason: lastReason || 'all channels failed', results };
}

module.exports = { CHANNELS, resolveChannels, notify };
```

- [ ] **Step 5: 跑测试确认通过**

```bash
node --test src/notify.test.js
```
期望：7 个用例全 PASS。

- [ ] **Step 6: `src/email.js` 改造 — `sendDailyDigest` 走 notify（签名不变）**

顶部 import 加一行：

```js
const notify = require('./notify');
```

`sendDailyDigest`（原 194-205 行）改为：

```js
async function sendDailyDigest(sections, opts = {}) {
  try {
    const filtered = filterDigestSections(sections);
    const payload = {
      subject: buildSubject(filtered),
      text: buildDigestText(filtered),
      html: buildDigestHtml(filtered),
    };
    // 单测 hook：opts.sender 直透 payload（不走 notify），保持既有 email.test.js 语义
    if (opts.sender) return await opts.sender(payload);
    // 生产路径：统一走通知分发器（默认 email 通道 → sendEmail）
    return await notify(payload, opts);
  } catch (err) {
    return { sent: false, reason: err.message };
  }
}
```

> 循环依赖说明：`email.js` 顶部 `require('./notify')` → notify 顶层不 require email（只在 `CHANNELS.email` 函数体内惰性 require）→ 加载顺序安全。加载 email 时 notify 已可解析；调用时 `require('./email')` 返回已完整的缓存。

- [ ] **Step 7: 回归 email 既有测试（关键：opts.sender hook 语义不破）**

```bash
node --test src/email.test.js
```
期望：现有 email 用例全 PASS（含「未配置返回 sent:false」「sender 透传 subject」「内部先过滤 T0/T1」）。若某用例因 notify 路径失败，检查该用例是否没传 `opts.sender` 且依赖旧的直连 sendEmail 返回值——应保持 `{sent, subject?}` 形状兼容。

- [ ] **Step 8: `scripts/run-pipeline.js` 告警改走 notify（顺带修旧签名误用）**

`sendAlertEmail`（原 47-69 行）改为 async + notify。⚠️ 原 `email.sendEmail(to, subject, htmlBody)` 用的是已被 email.js 改掉的旧签名（`sendEmail({subject,text,html})`），实际是静默失败——本步收口后告警才能真正送达。

```js
// ─── Alert (best-effort; 统一走 src/notify.js 分发器) ─────────────
async function sendAlertEmail(subject, htmlBody) {
  if (noAlert) return false;
  try {
    const notify = require('../src/notify');
    const r = await notify({ subject, html: htmlBody, text: '' });
    if (r.sent) console.log(`[pipeline] alert sent: ${subject}`);
    else console.warn(`[pipeline] alert failed (best-effort): ${r.reason}`);
    return r.sent;
  } catch (e) {
    console.warn('[pipeline] alert error:', e.message);
    return false;
  }
}
```

> 调用点 `sendAlertEmail(...)` 均不 await（fire-and-forget），但 Task 7 的 `finishLog` 需 `await sendAlertEmail(...)` 再 exit，见 Task 7 Step 2。语法检查通过即可（backend lint 只做 `node --check`）。

- [ ] **Step 9: 功能测试 — 全套回归**

```bash
npm run check
```
期望：全绿。

- [ ] **Step 10: Commit**

```bash
git add src/notify.js src/notify.test.js src/config.js src/email.js scripts/run-pipeline.js
git commit -m "feat(a3): 通知分发器 notify.js——日报与告警收口，多通道可配置；修复 run-pipeline 告警旧签名误用"
```

---

### Task 7: B1 日志收尾 flush — `run-pipeline.js` 保证 `exit code=0` 落盘后再退出

**Files:**
- Modify: `scripts/run-pipeline.js:209-234`（`child.on('error')` + `child.on('exit')`）
- Test: E2E 验证（跑一次真实管线，`tail` 日志收尾行）——不写自动化单测（run-pipeline 无可注入状态；真实跑即最直接验证）

**Interfaces:**
- Consumes: Task 6 的 `sendAlertEmail`（async）
- Produces: `run-pipeline.js` 日志文件末尾必有 `=== <ts> exit code=0 ===` 收尾行

- [ ] **Step 1: 先复现 bug（E2E 前行为快照）**

```bash
node scripts/run-pipeline.js --no-docker --no-alert
tail -3 logs/pipeline-$(date +%F).log
```
期望（bug 复现）：最后一行是 `=== <ts> exit code=0 ===` 或缺失该行（竞态下 `process.exit` 先于 flush 退出，日志截断在最后一行之前）。记录实际看到的结尾。

- [ ] **Step 2: 改写 exit/error 处理为「flush 回调内退出」**

把原 `child.on('error', ...)` 和 `child.on('exit', ...)`（209-234 行）整体替换：

```js
  // 日志收尾统一出口：write/end 回调链保证「收尾行落盘」后再打点、告警、退出，
  // 避免 process.exit 抢跑导致日志缺 `exit code=0` 结尾行（B1）。
  function finishLog(code, signal, spawnError) {
    const summary = spawnError
      ? `failed to spawn node: ${spawnError}`
      : `pipeline exited (code=${code}${signal ? ` signal=${signal}` : ''})`;
    log.write(`\n=== ${ts()} ${summary} ===\n\n`, () => {
      log.end(async () => {
        console.log(`[pipeline] ${summary}`);
        if (spawnError) {
          await sendAlertEmail('❌ ai-news-monitor: 管线启动失败', `<p>${ts()} ${summary}</p><p><small>— ai-news-monitor run-pipeline.js</small></p>`);
        } else if (code !== 0) {
          await sendAlertEmail(`❌ ai-news-monitor: 管线异常退出 (code=${code})`, `<p>${summary}</p><p>请检查日志: <code>logs/pipeline-${localStamp()}.log</code></p><p><small>— ai-news-monitor run-pipeline.js</small></p>`);
        }
        process.exit(code ?? 1);
      });
    });
    // 安全兜底：流异常时 5s 后强制退出，避免进程悬挂
    setTimeout(() => process.exit(code ?? 1), 5000).unref();
  }

  child.on('error', (e) => {
    console.error('[pipeline] failed to spawn node:', e.message);
    finishLog(1, null, e.message);
  });
  child.on('exit', (code, signal) => {
    finishLog(code ?? 1, signal, null);
  });
```

> `spawnError` 分支复用同一出口，日志也有收尾行；`setTimeout(...).unref()` 不阻止进程自然退出。

- [ ] **Step 3: E2E 验证修复**

```bash
node scripts/run-pipeline.js --no-docker --no-alert
tail -3 logs/pipeline-$(date +%F).log
```
期望：最后一行必为 `=== <ts> exit code=0 ===`（连续跑 2-3 次均稳定出现，不再竞态缺失）。

- [ ] **Step 4: Commit**

```bash
git add scripts/run-pipeline.js
git commit -m "fix(b1): 日志收尾 flush——write/end 回调链落盘后再 process.exit，消除 exit code 行缺失误判"
```

---

### Task 8: 端到端验收 + 文档收口

**Files:**
- Modify: `CLAUDE.md`（已知陷阱/关键约束同步）
- Modify: `docs/PROGRESS.md`（新增 F-021 条目）
- Test: 全链路 E2E（模块 → 功能 → 端到端，用户验收口径）

**Interfaces:**
- Consumes: Task 1-7 全部产物

- [ ] **Step 1: 全套检查**

```bash
npm run check
```
期望：lint / type-check / test 全绿（含 fetch-chain、seen、notify、index 新用例）。

- [ ] **Step 2: 真实端到端跑一轮（含 Docker）**

```bash
node scripts/run-pipeline.js
```
期望（对照清单逐项确认）：
1. 日志末尾 `=== <ts> exit code=0 ===`（B1）。
2. 首次跑后 `logs/.seen-ids.json` 生成，内容为 `{version:1, sources:{...}}`（A2）。
3. **连跑第二遍**：日志出现 `[Seen] N 条近期已分析，跳过` 且各源 `未处理` 显著减少（A2 ring 生效）；`[crawl4ai]`/`[twikit]` 小写通道日志（A1）。
4. 邮件摘要正常发送（A3 收口后走 notify 通道）；或在 `EMAIL_ENABLED=0` 下确认管线仍正常退出。
5. Anthropic T0 源（claude.com/anthropic.com）在 backends=单通道配置下仍能出内容（A1 配置无副作用）。

- [ ] **Step 3: score-0 放行行为抽查（execute_sql）**

```sql
-- 跑完两轮后，确认存在 score=0 行（已见标记仍在写），且 RPC 不再拦它们：
SELECT score, count(*) FROM public.articles WHERE created_at::date = current_date GROUP BY score;
-- 期望: score=0 与 score>=60 都有行（assembleRecords 仍写 0 分已见标记，但 A2-2 已放行重评）
```

- [ ] **Step 4: 文档收口 — CLAUDE.md**

「关键约束」节追加一条（放在 X 账号信源条目后）：

```markdown
- **抓取通道数据化（A1）**：`keyword_sources.backends`（jsonb，有序通道名数组）驱动每信源降级链，由 `src/fetch-chain.js` 执行（`crawl4ai`/`direct`/`twikit` 注册表）；NULL 走兼容默认（X→twikit 主链、其余→crawl4ai→direct）。新增/调优信源优先改 `backends` 数据而非代码
- **增量幂等闸（A2）**：`src/seen.js` 每源「最近 200 条已分析 URL」环形缓冲，持久化 `logs/.seen-ids.json`（gitignored），抓取后、DB 过滤前剔已见省 AI 预算；`get_new_urls` RPC 只把 score>0 视为已见——score-0 行可被重评（修复误杀永久固化），短期重评由 seen ring 兜住
- **通知分发器（A3）**：`src/notify.js` 统一 `notify({subject,text,html})`，通道由 `.env` 的 `NOTIFY_CHANNELS` 逗号分隔（默认 `email`，复用 `email.js`）；日报与 `run-pipeline.js` 失败告警均收口于此
```

「已知陷阱」节的 run-pipeline 条目补一句：**B1 已修**——日志收尾 `write/end` 回调链落盘后再 `process.exit`，`exit code=0` 结尾行稳定存在。

- [ ] **Step 5: 文档收口 — PROGRESS.md**

追加 F-021 条目：A1+A2+A3+B1 落地摘要（DECISION 链接 + 验收结果 + 遗留观察项：A4/A5/A6 延后、B2 待管理员）。

- [ ] **Step 6: Commit**

```bash
git add CLAUDE.md docs/PROGRESS.md
git commit -m "docs(architecture): F-021 收口——A1 通道数据化/A2 增量幂等/A3 通知分发/B1 日志flush 落地记录"
```

- [ ] **Step 7: 给用户的验收报告**

汇总：每个措施的模块测试通过数、功能测试（npm run check）结果、端到端清单逐项确认（1-5 + score-0 抽查）、遗留项（A4/A5/A6/B2、push 到 origin/master）。

---

## Self-Review 记录

- **Spec 覆盖**：A1（Task1+2）、A2（Task3+4+5）、A3（Task6）、B1（Task7）、E2E+文档（Task8）——REQ 采纳范围 4 项全覆盖；A4/A5/A6/B2 明确不进本 plan（DECISION 已记录延后）。
- **占位符扫描**：所有代码步骤含完整实现，无 TBD/TODO/「仿 Task N」。
- **类型一致性**：`fetchSourceWithChain(source, registry?)` 在 Task2 定义、Task2 内使用一致；`SeenStore.load({filePath, capacity})`/`has(sourceKey, urlKey)`/`add(...)`/`save({filePath})` 在 Task3 定义、Task4 一致调用；`notify(payload, opts)` 返回 `{sent, subject?, reason?, results?}` 在 Task6 定义、email.js/run-pipeline 消费一致；`sendDailyDigest` 对外 `{sent, subject?, reason?}` 签名不变（index.js 依赖）。
