# REVIEW: {任务简述}

> 复核日期: YYYY-MM-DD ｜ 复核 agent: {subagent 标识}
> 对应 PLAN: docs/PLAN-xxx.md ｜ 风险等级: 🟢 低 / 🟡 中 / 🔴 高

## 复核范围

- [ ] 代码改动符合 PLAN 范围（无范围外改动）
- [ ] npm run check 全绿（lint + type-check + 所有单测）
- [ ] 无 .env* 泄露
- [ ] 无危险操作（删数据/改 DDL/改 RLS/改 settings.json）——如涉及高危项，逐项核对
- [ ] 新增测试覆盖关键路径
- [ ] 回归测试通过（相关模块不受影响）

## 复核结果

| 检查项 | 状态 | 证据 |
|--------|------|------|
| check 全绿 | ✅/❌ | 命令输出截取 |
| 范围合规 | ✅/❌ | git diff --stat |
| 测试覆盖 | ✅/❌ | 测试用例清单 |
| 密钥安全 | ✅/❌ | 无 .env* 变更 |
| 已知陷阱 | ✅/❌ | 与 KNOW_TRAPS.md 对照 |

## 判定

- [ ] ✅ PASS — 可以标记完成，更新 PROGRESS.md
- [ ] ⚠️ FAIL_WITH_ISSUES — 有非阻塞问题，记录后放行（问题清单见下）
- [ ] 🚫 BLOCKED — 阻塞性问题，必须修复后重新复核

## 问题清单（如有）

1. [问题描述 + 严重程度 + 修复建议]
